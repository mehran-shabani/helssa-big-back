"""
تنظیمات اپلیکیشن احراز هویت OTP
OTP Authentication App Configuration
"""

from django.apps import AppConfig


class AuthOtpConfig(AppConfig):
    """
    پیکربندی اپلیکیشن auth_otp
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'auth_otp'
    verbose_name = 'احراز هویت OTP'
    
    def ready(self):
        """
        یک‌خطی: نقطهٔ ورود برای پیکربندی زمان بارگذاری اپلیکیشن.
        
        توضیح:
        این متد توسط فریم‌ورک جنگو هنگام آماده‌شدن رجیستری اپلیکیشن فراخوانی می‌شود و محل مناسب برای انجام مقداردهی‌های زمان اجرا و ثبت‌کننده‌های رویداد (مثل وارد کردن و ثبت سیگنال‌ها) است. در این پیاده‌سازی فعلی هیچ عملی انجام نمی‌شود (pass) — اگر می‌خواهید گیرنده‌های سیگنال یا دیگر تنظیمات startup را ثبت کنید، آن‌ها را در اینجا وارد کنید (مثلاً `import auth_otp.signals`) تا هنگام بارگذاری اپلیکیشن اجرا شوند.
        
        نکته:
        از انجام عملیات زمان‌بر یا وابسته به درخواست‌های I/O طولانی در این متد خودداری کنید؛ تنها کارهای سبک و ضروریِ راه‌اندازی را انجام دهید.
        """
        # می‌توانید سیگنال‌ها را اینجا import کنید
        # import auth_otp.signals
        pass
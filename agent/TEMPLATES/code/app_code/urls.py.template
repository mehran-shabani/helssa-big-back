from django.urls import path
from . import views


urlpatterns = [
    path("health/", views.health, name="{{APP_NAME}}-health"),
    path("entities/", views.ExampleEntityListCreateView.as_view(), name="{{APP_NAME}}-entities"),
    path("entities/<int:pk>/", views.ExampleEntityRetrieveUpdateDestroyView.as_view(), name="{{APP_NAME}}-entity-detail"),
]
class Orchestrator:
    """Coordinates cores based on the app PLAN and business rules."""

    def __init__(self, text_processor=None, speech_processor=None):
        self.text_processor = text_processor
        self.speech_processor = speech_processor

    def run(self, request_payload: dict) -> dict:
        # TODO: implement orchestration per PLAN.md
        return {"result": "ok", "payload": request_payload}